{
  "name": "smart-home-simulation",
  "private": true,
  "scripts": {
    "start": "echo \"Use 'java -jar target/smart-home-simulation-1.0.0.jar' to run the application\" && exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}